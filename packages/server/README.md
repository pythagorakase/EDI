# EDI Server

Server-side code for the EDI communication system. Runs on the headless Linux server that hosts Clawdbot.

## Structure

```
server/
├── README.md       # This file
└── ...             # Server-side handlers (TBD)
```

## Deployment

This code runs on `claude-base.tail342046.ts.net` and handles incoming messages from the EDI CLI client.
